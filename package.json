{
  "name": "ghp-tooling",
  "private": true,
  "description": "Convenience scripts, git hooks, and commit tooling for the ghp repository (Husky + commitlint). The framework itself is Go — see go.mod.",
  "scripts": {
    "prepare": "husky",
    "dev": "go run ./src/cmd/ghp dev",
    "build": "go build -o bin/ghp ./src/cmd/ghp",
    "start": "./bin/ghp help",
    "format": "gofmt -w ./src",
    "coverage": "go test ./src/... -coverprofile=coverage.out -covermode=atomic -coverpkg=./src/... && go tool cover -func=coverage.out | grep total:"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  }
}
