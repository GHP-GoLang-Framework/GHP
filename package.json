{
  "name": "ghp-tooling",
  "private": true,
  "description": "Scripts de conveniência, git hooks e ferramental de commit para o repositório ghp (Husky + commitlint). O framework em si é Go — ver go.mod e Makefile.",
  "scripts": {
    "prepare": "husky",
    "dev": "go run ./src/cmd/ghp dev",
    "build": "go build -o bin/ghp ./src/cmd/ghp",
    "start": "./bin/ghp help"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  }
}
