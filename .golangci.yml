run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - errcheck
    - gofmt
    - goimports
    - misspell

issues:
  exclude-dirs:
    - test/e2e
    - test/integration
